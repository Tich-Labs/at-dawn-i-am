<div class="max-w-3xl mx-auto px-6 py-16">
  <header class="mb-12 text-center">
    <h1 class="text-4xl font-serif text-stone-800 mb-4"><%= @about_page.title %></h1>
  </header>
  <div class="prose prose-lg mx-auto max-w-none font-serif text-stone-700 leading-relaxed">
    <%= simple_format(@about_page.content) %>
  </div>
  <nav class="mt-16 text-center">
    <%= link_to "← Back to Journal", root_path, class: "text-sm text-stone-400 hover:text-stone-600 transition-colors" %>
  </nav>
</div>
